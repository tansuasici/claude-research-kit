% Minimal example manuscript demonstrating the kit's layout.
% With the kit active, citation-gate.sh passes on this file:
%   - every \cite resolves in references.bib
%   - every \ref has a \label
\documentclass[11pt]{article}
\usepackage{graphicx}
\usepackage[numbers]{natbib}

\title{A verification gate for tool-augmented LLM agents: a minimal example}
\author{Example Author}

\begin{document}
\maketitle

\begin{abstract}
This skeleton shows the intended file layout. Replace every sentence with
source-grounded prose. Numbers in this abstract must reappear, identical, in
Results --- the kit checks for that during verification.
\end{abstract}

\input{sections/intro}

\section{Method}
The agent and its pre-execution verification gate are shown in
Figure~\ref{fig:arch}. Describe it for reproduction: the base model, the tool
set, the gating rule, and the decoding settings.

\begin{figure}[t]
  \centering
  % \includegraphics{figures/arch.pdf}
  \caption{Agent with a pre-execution verification gate on tool calls.}
  \label{fig:arch}
\end{figure}

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
